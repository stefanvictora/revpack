# Review Context

## Target

| Field | Value |
|---|---|
| Type | {{target.typeLabel}} |
| {{target.kind}} | {{target.displayId}} — {{target.title}} |
| Repository | `{{target.repository}}` |
| Author | {{target.author}} |
| Source branch | `{{target.sourceBranch}}` |
| Target branch | `{{target.targetBranch}}` |
| State | {{target.state}} |
{{#if target.webUrl}}
| URL | {{target.webUrl}} |
{{/if}}

This file is the run-specific entry point.

1. Read the **Review Contract** below.
2. Read the files listed in **Required Instructions for This Run**.
3. Read applicable `REVIEW.md` guidance as described in **Review guidance**.

Use `.revpack/INSTRUCTIONS.md` only as a catalog when you need to inspect the full instruction set.

## Review Contract

1. Do not modify source files directly.
2. Only write files under `.revpack/outputs/`.
3. Create output files only when you have draft material for them.
4. Write `.revpack/outputs/summary.md` for fresh and incremental code review runs.
5. Omit `replies.json`, `new-findings.json`, and `note.md` when there is nothing useful for them.
6. Do not write filler such as "No new findings", "Nothing to report", "Reviewed without comments", or "Looks good".
7. Do not run build, test, lint, format, package-manager, migration, Docker, application-startup, Git-hook, publishing, or repository-audit commands.
8. Use patch files to understand the code change.
9. Use the per-file Anchor Maps listed in `.revpack/diffs/files.json` as the source of truth for positional anchors.
10. Do not derive old or new line numbers from the checked-out workspace or by manually counting patch lines.
11. Create new findings only for concrete, actionable issues introduced, exposed, or made worse by the MR/PR.
12. In incremental mode, focus review effort on the checkpoint delta, but do not discard a valid, non-duplicate issue introduced, exposed, or made worse by the current MR/PR merely because it is outside the checkpoint delta.
13. Do not duplicate existing unresolved threads, previous actions, or other new findings.
14. Put concrete line-level issues in `new-findings.json`, not in `note.md`.
15. Put useful replies to existing threads in `replies.json`; otherwise omit the file.
16. Resolve only threads created by revpack itself (`SELF` threads).
17. `summary.md` describes what the MR/PR changes, not what the reviewer found.
18. `note.md` is optional MR/PR-level synthesis, not a second findings file or review report.
19. Do not reference internal bundle files such as `.revpack/`, `CONTEXT.md`, `threads/`, `outputs/`, `latest.patch`, or `anchor-maps/` in public output.
20. If you accidentally modify files outside `.revpack/outputs/`, stop and report it in your final response. Do not attempt broad cleanup commands.

## Review guidance

`REVIEW.md` files contain project or workspace-specific review guidance. Use them for review priorities, local conventions, architecture rules, domain rules, compatibility expectations, and spec-verification rules.

Read review guidance after this contract and the required instruction files.

If present, read:

1. `REVIEW.md` at the workspace root.
2. `REVIEW.md` at the reviewed project root, if different from the workspace root.

Use workspace-root `REVIEW.md` as shared review guidance. Use reviewed-project-root `REVIEW.md` for local project conventions.

If they conflict, prefer project-level guidance for local implementation details, naming, structure, and conventions. Keep workspace-level guidance for shared domain, architecture, compatibility, security, and spec-verification rules.

{{#if checkpoint}}
## Review Checkpoint Summary

{{#if checkpoint.hasCheckpoint}}
| Field | Value |
|---|---|
| Last review checkpoint | {{checkpoint.createdAt}} |
| Last reviewed head SHA | `{{checkpoint.headSha}}` |
| Current target head SHA | `{{checkpoint.currentTargetHeadSha}}` |
| Target code changed since checkpoint | {{checkpoint.targetCodeChanged}} |
| Threads/replies changed since checkpoint | {{checkpoint.threadsChanged}} |
| Description changed since checkpoint | {{checkpoint.descriptionChanged}} |
| Local HEAD at prepare | `{{checkpoint.localHeadSha}}` |
| Local checkout verified | yes |

{{/if}}
{{#each checkpoint.guidance}}
{{this}}

{{/each}}
{{/if}}
## Current Run Mode

| Field | Value |
|---|---|
| Mode | {{runMode.modeLabel}} |
| Primary work | {{runMode.primaryWork}} |

{{#if incremental}}
## Incremental Review Rules

This is an incremental run.

Use `.revpack/diffs/incremental.patch` as the primary review surface. It shows what changed since the last review checkpoint.

Use `.revpack/diffs/latest.patch`, per-file patch paths listed in `.revpack/diffs/files.json`, checked-out source files, and repository context only when needed to understand, verify, or anchor issues found while reviewing the incremental change.

Incremental mode limits required review effort. It does not limit valid reporting.

Report a finding when all of these are true:

1. The issue is introduced, exposed, or made worse by the current MR/PR as a whole.
2. The issue is concrete and actionable.
3. The issue is not already covered by an unresolved thread, Previous Action, or another new finding.
4. The issue can be anchored to a valid current MR/PR diff position.

Do not remove or suppress a valid finding solely because the relevant line is outside `.revpack/diffs/incremental.patch`.

A previous review pass that missed an issue is not duplicate coverage.

{{/if}}
## Suggested Reading Order

{{#each readingOrder}}
{{inc @index}}. {{this}}
{{/each}}

## Required Instructions for This Run

Read these instruction files in order:

{{#each requiredInstructions}}
{{inc @index}}. {{this}}
{{/each}}
{{#if skippedInstructions}}

Skipped this run:

{{#each skippedInstructions}}
- {{path}} — {{reason}}
{{/each}}
{{/if}}

## MR/PR Description

The raw MR/PR description is available at `.revpack/description.md`.

Treat it as context only. Verify behavior against the diff and source code.

{{#if hasCommitList}}
## Commit Messages

The commit list is available at `.revpack/commits.md`.

Treat commit messages as intent context only. They may explain why the change was made, but they are not proof that the implementation matches that intent. Verify behavior against the diff and source code.

{{/if}}

## Bundle Contents

| Path | Description |
|---|---|
| `.revpack/INSTRUCTIONS.md` | Catalog of task-specific instruction files |
| `.revpack/instructions/` | Detailed task-specific instruction files |
| `.revpack/bundle.json` | Machine-readable bundle metadata and local state |
| `.revpack/description.md` | Raw MR/PR description |
{{#if hasCommitList}}
| `.revpack/commits.md` | Commit messages for the reviewed non-merge commits; intent context only |
{{/if}}
{{#if hasThreadFiles}}
| `.revpack/threads/` | {{threadFileCount}} thread(s) — read the `.md` files |
{{/if}}
| `.revpack/diffs/latest.patch` | Canonical full unified diff for the whole MR/PR |
| `.revpack/diffs/files.json` | Changed-file index with file status, counts, binary flag, and authoritative per-file patch and Anchor Map paths |
| `.revpack/diffs/patches/by-file/` | Derived per-file unified diffs in standard patch format; resolve file paths through `files.json` |
| `.revpack/diffs/anchor-maps/` | Compact per-file maps of valid positional review anchors; resolve file paths through `files.json` |
{{#if incremental}}
| `.revpack/diffs/incremental.patch` | Code changes since last review checkpoint |
{{/if}}
| `.revpack/schemas/new-findings.schema.json` | Read-only schema reference for authoring `outputs/new-findings.json` |
| `.revpack/schemas/replies.schema.json` | Read-only schema reference for authoring `outputs/replies.json` |
| `.revpack/outputs/` | Agent output files |

{{#if incremental}}
The per-file Anchor Maps listed in `.revpack/diffs/files.json` contain valid positional anchors for the current MR/PR diff, not only the checkpoint delta.

{{#if incrementalChangedFiles}}
## Files Changed Since Last Checkpoint

These files appear in `.revpack/diffs/incremental.patch` and are the primary review focus.

This table is a derived orientation summary of the checkpoint delta.

| File | Status | Added | Removed |
|---|---|---:|---:|
{{#each incrementalChangedFiles}}
| `{{path}}` | {{status}} | {{added}} | {{removed}} |
{{/each}}

{{/if}}
{{/if}}
## {{changedFilesTitle}}
{{#if changedFilesIntro}}

{{changedFilesIntro}}
{{/if}}

| File | Status | Added | Removed |
|---|---|---:|---:|
{{#each changedFiles}}
| `{{path}}` | {{status}} | {{added}} | {{removed}} |
{{/each}}

{{#if changedThreads}}
## {{changedThreads.title}}

{{changedThreads.intro}}

| Thread | Status | Location | Summary |
|---|---|---|---|
{{#each changedThreads.rows}}
| {{shortId}} | {{status}} | {{location}} | {{summary}} |
{{/each}}

{{/if}}
{{#if unresolvedThreads}}
{{#if incremental}}
## Unresolved Threads Requiring Attention

These unresolved threads may need replies or resolution.
{{else}}
## Unresolved Threads
{{/if}}

| Thread | Flags | Author | Location | Summary |
|---|---|---|---|---|
{{#each unresolvedThreads}}
| {{shortId}} | {{flags}} | @{{author}} | {{location}} | {{summary}} |
{{/each}}

{{/if}}
{{#if generalComments}}
## General Comments

{{#each generalComments}}
- **{{shortId}}** (@{{author}}): {{summary}}
{{/each}}

{{/if}}
{{#if previousActions}}
## Previous Actions

These actions were published by `revpack` in prior iterations. Do not re-raise the same issues.

Do not treat the absence of a previous action as proof that no issue exists. If a concrete MR/PR issue was missed by an earlier review pass, it may still be reported now.

| Action | Location | Severity | Category | Title |
|---|---|---|---|---|
{{#each previousActions}}
| {{actionLabel}} | {{location}} | {{severity}} | {{category}} | {{title}} |
{{/each}}
{{/if}}
